package com.shevelev.wizard_camera.common_entities.enums

/**
 * System filter codes
 */
enum class SystemFilterCode(val value: Int) {
    BRIGHTNESS(0),
    CONTRAST(1),
    SATURATION(2),
    TEMPERATURE(3)
}